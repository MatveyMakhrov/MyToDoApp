<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Активное состояние (включено) -->
    <item android:drawable="@color/switch_active" android:state_checked="true" />
    <!-- Неактивное состояние (выключено) -->
    <item android:drawable="@color/switch_inactive" />
</selector>